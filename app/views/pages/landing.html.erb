<h1>Pages#landing</h1>
<p>Find me in app/views/pages/landing.html.erb</p>

<div class="row">
  <div class="col-md-6 offset-md-2">
<table class="table table-hover">
  <thead>
    <tr>
      <th class="table-active">Name</th>
      <th class="table-active">Age</th>
      <th class="table-active">Email</th>
    </tr>
  </thead>

  <tbody>

    <% @students.each do |student| %>

      <tr>
        <td class="table-active"><%= student.name %></td>
        <td class="table-active"><%= student.age %></td>
            <td class="table-active"><%= student.email %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
